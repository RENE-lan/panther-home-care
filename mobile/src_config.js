// ⚙️  IMPORTANT — set this to your backend address:
//   • Local dev on a phone: your computer's LAN IP, e.g. http://192.168.1.20:8000
//     (find it with `ipconfig` on Windows — the IPv4 address). The phone and PC
//     must be on the same Wi-Fi. Do NOT use 127.0.0.1 (that's the phone itself).
//   • Deployed: your Render URL, e.g. https://panther-backend-xxxx.onrender.com
export const API_BASE = "http://192.168.1.20:8000/api/mobile";
